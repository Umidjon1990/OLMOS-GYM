{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "/root/.local/share/pnpm/pnpm run build:railway"
  },
  "deploy": {
    "releaseCommand": "DATABASE_URL=$DATABASE_URL /root/.local/share/pnpm/pnpm --filter @workspace/db run push-force || npx --yes drizzle-kit push --force --config lib/db/drizzle.config.ts",
    "startCommand": "node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
